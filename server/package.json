{
  "name": "work-order-server",
  "version": "1.0.0",
  "private": true,
  "description": "Option B — Work Order MES API (Express + node:sqlite)",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "jest --runInBand"
  },
  "dependencies": {
    "express": "^4.21.2"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  }
}
